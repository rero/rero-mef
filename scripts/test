#!/usr/bin/env bash
# SPDX-FileCopyrightText: Fondation RERO+
# SPDX-License-Identifier: AGPL-3.0-or-later

PROGRAM=`basename $0`
SCRIPT_PATH=`dirname $0`

# Colored message helpers: msg, info_msg, error_msg, error_msg+exit,
# success_msg, colored_msg.
. "$(dirname "${BASH_SOURCE[0]}")/messages"

# Displays program name-> Vulnerability found in py version 1.11.0
msg "PROGRAM: ${PROGRAM}"

# uv is a mandatory condition to launch this program!
if [[ -z "${VIRTUAL_ENV}" ]]; then
  error_msg+exit "Error - Launch this script via uv command:\n\uv run run-tests"
fi

pip_audit_exceptions=()
add_exceptions() {
  pip_audit_exceptions+=(--ignore-vuln "$1")
}

set -e

# Add exceptions below when a known vulnerability cannot yet be fixed.
# Format: add_exception "VULN-ID"  # reason and upstream issue link
info_msg "Check vulnerabilities:"
# urrlib3 is pinned because of ES compatibility until migration to OpenSearch
# urllib3 1.26.20 GHSA-pq67-6m6q-mj2v 2.5.0
add_exceptions "GHSA-pq67-6m6q-mj2v"
# urllib3 1.26.20 CVE-2025-66418 2.6.0
add_exceptions "CVE-2025-66418"
# urllib3 1.26.20 CVE-2025-66471 2.6.0
add_exceptions "CVE-2025-66471"
# urllib3   1.26.20 CVE-2026-21441 2.6.3
add_exceptions "CVE-2026-21441"
# urllib3 1.26.20 CVE-2026-44431 2.7.0
add_exceptions "CVE-2026-44431"
# pytest needs to be updated by pytest-invenio
# pytest 8.4.2   CVE-2025-71176 9.0.3
add_exceptions "CVE-2025-71176"
# sqlitedict 2.1.0   PYSEC-2026-1939
add_exceptions "PYSEC-2026-1939"
# click will be updated when python-semantic-release is extracted from the dev dependencies
# click 8.1.8   PYSEC-2026-2132 8.3.3
add_exceptions "PYSEC-2026-2132"
pip-audit "${pip_audit_exceptions[@]}"

info_msg "Test formatting:"
ruff format . --check
info_msg "Test linting:"
ruff check
info_msg "Tests:"
pytest --cov=rero_mef --cov-report=term-missing

success_msg "Perfect ${PROGRAM}! See you soon…"
exit 0
